# LineStyle

```
interface LineStyle {
  width: Uint(16);
  startCap: CapStyle;
  endCap: CapStyle;
  join: JoinStyle;
  noHScale: Boolean;
  noVScale: Boolean;
  noClose: Boolean;
  pixelHinting: Boolean;
  fill: FillStyle;
}
```
