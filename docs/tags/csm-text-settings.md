# CsmTextSettings

```
interface CsmTextSettings variantof Tag(type) {
  type: TagType.CsmTextSettings;
  textId: Uint(16);
  renderer: TextRenderer;
  fitting: GridFitting;
  thickness: Float(32);
  sharpness: Float(32);
}
```
