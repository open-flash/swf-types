# TextRecord

```
interface TextRecord {
  fontId: Option(Uint(16));
  color: Option(StraightSRgba8);
  offsetX: Sint(16);
  offsetY: Sint(16);
  fontSize: Option(Uint(16));
  entries: GlyphEntry[];
}
```
