# ClipEventFlags

```
interface ClipEventFlags {
  keyUp: Boolean;
  keyDown: Boolean;
  mouseUp: Boolean;
  mouseDown: Boolean;
  unload: Boolean;
  enterFrane: Boolean;
  load: Boolean;
  dragOver: Boolean;
  rollOut: Boolean;
  rollOver: Boolean;
  releaseOutside: Boolean;
  release: Boolean;
  press: Boolean;
  initialize: Boolean;
  data: Boolean;
  construct: Boolean;
  keyPress: Boolean;
  dragOut: Boolean;
}
```
